#include<stdio.h>
/**
 * main - print alphabet exclude q,e
 *
 * Return: =0
 */
int main(void)
{
	char ch;

	for (ch = 'a'; ch <= z ; ch++)
	{
		if (a == 'q' || a == 'e')
		{
			continue;
		}
		putchar(ch);
	}
	putchar('\n');
	return (0);
}
